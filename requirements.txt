# Core dependencies, added as each build step requires them (see CLAUDE.md
# Section 11 for the build order). Versions are pinned to what was verified
# working during development.

# Step 2: embedding + dense passage index
sentence-transformers==6.0.1
faiss-cpu==1.15.0
numpy==2.5.3

# Step 3: generation (frozen LLM)
openai==3.14.0
python-dotenv==1.2.3

# Step 4: evaluation harness
pandas==3.0.5

# Step 5: agent orchestration (Systems B and C)
langgraph==1.2.11

# Step 7: knowledge-graph construction
networkx==3.6.1
